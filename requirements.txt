# PulkFit 2.0 — Python dependencies
# Install locally:   pip install -r requirements.txt
# Streamlit Cloud:   commit this file; it installs automatically on deploy

# Core
supabase==2.10.0
pandas>=2.2.0
python-dotenv==1.0.1
requests==2.32.3

# Streamlit dashboard
streamlit==1.37.0
plotly==5.22.0
altair==5.3.0

# Fitbit OAuth (bootstrap only — not needed at runtime on Streamlit Cloud)
# The bootstrap script uses Python's built-in http.server + webbrowser
# No extra OAuth library required.
